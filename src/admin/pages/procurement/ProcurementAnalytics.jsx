import React from 'react';
import { ADMIN_STYLES } from '../../styles/adminStyles';

const ProcurementAnalytics = () => {
  return (
    <div className={ADMIN_STYLES.sectionSpacing}>
      <div className={ADMIN_STYLES.glassCard}>
        <h1 className={ADMIN_STYLES.pageTitle}>成本分析</h1>
        <p className={ADMIN_STYLES.pageSubtitle}>採購成本分析和報表開發中...</p>
      </div>
    </div>
  );
};

export default ProcurementAnalytics;